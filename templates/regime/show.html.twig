{% extends 'base.html.twig' %}

{% block title %}Regime{% endblock %}

{% block body %}
<div class="container">
    <h1>Afficher le type de régime</h1>

    <table class="table">
        <tbody>
            <tr>
                {# <th>Id</th>
                <td>{{ regime.id }}</td> #}
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ regime.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a  class="btn" href="{{ path('app_regime_index') }}">Précédent</a>

    <a  class="btn" href="{{ path('app_regime_edit', {'id': regime.id}) }}">Modifier</a>

    {{ include('regime/_delete_form.html.twig') }}
</div>
<style>
.btn {
    background-color:#609A7D;
     color:FFFFFF;
     margin-top:2%;
       
}
.btn:hover {
  color: black;
}
h1 {
    font-family: Arial;
    text-align: center;
}
</style>
{% endblock %}
