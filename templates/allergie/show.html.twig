{% extends 'base.html.twig' %}

{% block title %}Allergie{% endblock %}

{% block body %}
<div class="container">
    <h1>Afficher l'allergie</h1>

    <table class="table">
        <tbody>
            <tr>
                {# <th>Id</th>
                <td>{{ allergie.id }}</td> #}
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ allergie.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn" href="{{ path('app_allergie_index') }}">Précédent</a>

    <a class="btn" href="{{ path('app_allergie_edit', {'id': allergie.id}) }}">Modifier</a>

    {{ include('allergie/_delete_form.html.twig') }}

</div>
<style>
.btn {
    background-color:#609A7D;
     color:FFFFFF;
     margin-top:2%;
       
}
.btn:hover {
  color: black;
}
h1 {
    font-family: Arial;
    text-align: center;
}
</style>
{% endblock %}
